<?xml version="1.0" encoding="utf-8"?>
<!-- fragment_admin_users.xml / fragment_admin_blocs.xml / fragment_admin_salles.xml / fragment_admin_fingerprints.xml -->
<!-- Tous les fragments admin partagent le même layout de base -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_gradient"
    android:padding="16dp">

    <!-- Barre recherche + bouton ajouter -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="14dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:background="@drawable/bg_glass_input"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:layout_marginEnd="10dp">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@android:drawable/ic_menu_search"
                android:tint="#607080"
                android:contentDescription="Rechercher"/>

            <EditText
                android:id="@+id/etAdminSearch"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:layout_marginStart="10dp"
                android:background="@null"
                android:hint="Rechercher..."
                android:textColorHint="#607080"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:singleLine="true"/>
        </LinearLayout>

        <!-- Bouton ajouter -->
        <LinearLayout
            android:id="@+id/btnAdd"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_button_cyan"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="+"
                android:textColor="#1A1A2E"
                android:textSize="22sp"
                android:textStyle="bold"/>
        </LinearLayout>
    </LinearLayout>

    <!-- Compteur -->
    <TextView
        android:id="@+id/tvCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0 éléments"
        android:textColor="#607080"
        android:textSize="12sp"
        android:layout_marginBottom="10dp"/>

    <!-- Progress -->
    <ProgressBar
        android:id="@+id/progressAdmin"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="center_horizontal"
        android:indeterminateTint="#00D4FF"
        android:visibility="gone"
        android:layout_marginBottom="8dp"/>

    <!-- Liste -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerAdmin"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="none"/>

</LinearLayout>